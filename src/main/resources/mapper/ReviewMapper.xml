<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
   <mapper namespace="com.team2.finalproject.mapper.ReviewMapper">
   
   	<select id="insertReview"
   			parameterType="com.team2.finalproject.dto.user.ReviewDto">
   	INSERT INTO REVIEW(review_no, user_no, product_code, product_name, title, rate
   					, purpose, agegroup, rdate, content)
   	VALUES(review_file_no_seq.NEXTVAL, #{userNo}, #{productCode}, #{productName},
   			#{title}, #{rate}, #{purpose}, #{agegroup}, sysdate, #{content})
   	</select>
   
   
   </mapper>